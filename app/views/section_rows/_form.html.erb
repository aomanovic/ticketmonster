<%= form_for(@section_row) do |f| %>
  <% if @section_row.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@section_row.errors.count, "error") %> prohibited this section_row from being saved:</h2>

      <ul>
      <% @section_row.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.number_field :capacity %>
  </div>
  <div class="field">
    <%= f.label :section_id %><br>
    <%= f.number_field :section_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
