<div class="row">
  <div class="col-md-6 col-md-offset-3">
<%= form_for(@venue_layout) do |f| %>
  <% if @venue_layout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@venue_layout.errors.count, "error") %> prohibited this venue_layout from being saved:</h2>

      <ul>
      <% @venue_layout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.label :venue_id %>
    <%= f.select :venue_id, options_from_collection_for_select(@venues, 'id', 'name'), class: 'form-control'%>

    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>

    <%= f.label :capacity %>
    <%= f.number_field :capacity, class: 'form-control' %>

    <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
</div>
</div>